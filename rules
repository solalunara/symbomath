#DistributeAddition
	m * ( a + b ) -> m * a + m * b
#FactorAddition
	m * a + m * b -> m * ( a + b )
#PreferAddition
	( a + b ) * / c -> a * / c + b * / c
	( a * d + b * c ) * / ( c * d ) ->  a * / c + b * / d
#PreferMultiplication
	a * / c + b * / c -> ( a + b ) * / c
	a * / c + b * / d -> ( a * d + b * c ) * / ( c * d )